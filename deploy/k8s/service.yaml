apiVersion: v1
kind: Service
metadata:
  name: velox
  namespace: velox
  labels:
    app.kubernetes.io/name: velox
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: velox
